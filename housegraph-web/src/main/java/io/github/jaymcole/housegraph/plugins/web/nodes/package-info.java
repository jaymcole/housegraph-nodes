/**
 * Nodes exposing the local web-hosting integration to the graph. Currently the
 * web-server resource node ({@code WebServerNode}), which serves a directory of static
 * files as {@code <name>.local} on the LAN. Backs onto the {@code plugins.web} package and
 * {@code ResourceRegistry}.
 */
package io.github.jaymcole.housegraph.plugins.web.nodes;
