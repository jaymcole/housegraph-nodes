// arduino_secrets.h.example
// Copy this file to `arduino_secrets.h` and fill in your Wi-Fi credentials.
// `arduino_secrets.h` is gitignored so real credentials stay out of the repo.
#define SECRET_SSID "your-wifi-name"
#define SECRET_PASSWORD "your-wifi-password"
