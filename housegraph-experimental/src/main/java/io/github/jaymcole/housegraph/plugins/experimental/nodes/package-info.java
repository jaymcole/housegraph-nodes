/**
 * Fun, exploratory nodes that exist to be looked at rather than to do real work — e.g. making a
 * graph's flow visible on the canvas instead of only inferable from logs. No shared mechanics
 * beyond that; each node here stands alone.
 */
package io.github.jaymcole.housegraph.plugins.experimental.nodes;
