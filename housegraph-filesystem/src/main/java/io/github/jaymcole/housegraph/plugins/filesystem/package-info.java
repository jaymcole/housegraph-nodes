/**
 * Folder management under HouseGraph's default storage location.
 * {@link io.github.jaymcole.housegraph.plugins.filesystem.RelativeFolder} resolves a caller-typed,
 * OS-agnostic subdirectory path under a root and creates it on demand, without ever escaping
 * that root.
 */
package io.github.jaymcole.housegraph.plugins.filesystem;
